// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBq-gvaraf4VoMxxoDjpiqVlDLjRTysaec",
    authDomain: "buy-and-sell-listings.firebaseapp.com",
    projectId: "buy-and-sell-listings",
    storageBucket: "buy-and-sell-listings.appspot.com",
    messagingSenderId: "725564486247",
    appId: "1:725564486247:web:80118c222b4fff73a59ac2",
    measurementId: "G-SS73DBBCEZ"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
